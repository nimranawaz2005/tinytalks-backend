{
  "name": "cry-diagnostic-backend",
  "version": "1.0.0",
  "description": "Secure backend proxy that forwards audio to Hugging Face without exposing the API key to the browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
